# MealSlot D3 — Combined Pytest Results

**Summary:** 114 tests executed — 104 PASS, 10 FAIL  
**Product behavior changes:** None

## Results

| Test | Why we tried it | Expected | What happened |
|---|---|---|---|
| `test_spinning_returns_one_dish_per_reel_from_the_offered_candidates` | UC1 — Spin for a Meal: This proves that a spin with one candidate per slot deterministically returns that candidate for every slot, matching the documented main success scenario. | Spinning returns one dish per reel from the offered candidates. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_slot_with_no_matching_dishes_gets_a_no_options_placeholder_instead_of_failing` | UC1 — Spin for a Meal: This proves that an empty reel (e.g. every dish filtered out) yields Extension 4b's placeholder dish rather than aborting the whole spin. | A slot with no matching dishes gets a no options placeholder instead of failing. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_spinning_again_before_the_cooldown_expires_is_rejected` | UC1 — Spin for a Meal: This proves that a spin request made inside the 3-second cooldown window is disallowed, matching Extension 7a, while one made after the window elapses is allowed. | Spinning again before the cooldown expires is rejected. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_merging_preferences_unions_allergens_and_keeps_the_strictest_diet` | UC2 — Set Party Dietary Preferences and Resolve Conflicts: This proves that merging two members' preferences picks the strictest diet, unions their allergens, and takes the minimum budget/time band, exactly as documented in the main success scenario. | Merging preferences unions allergens and keeps the strictest diet. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_mixing_vegan_and_omnivore_preferences_never_actually_raises_a_conflict_flag` | UC2 — Set Party Dietary Preferences and Resolve Conflicts: This proves (and documents a discrepancy) that the real mergeConstraints() algorithm always resolves diet-only combinations to one strictest diet, so Extension 4a's "mutually incompatible diets" conflict path is unreachable dead code in the current implementation, not an active safeguard. | Mixing vegan and omnivore preferences never actually raises a conflict flag. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_vegan_diet_combined_with_three_or_more_blocked_allergens_is_flagged_as_a_conflict` | UC2 — Set Party Dietary Preferences and Resolve Conflicts: This proves that a merged vegan diet plus four commonly-blocked allergens correctly triggers Extension 4b's conflict flag and suggestion. | Vegan diet combined with three or more blocked allergens is flagged as a conflict. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_keep_votes_reaching_a_majority_of_connected_members_lock_the_slot` | UC3 — Vote on a Spin Result: This proves that 2 keep-votes out of 3 live members (quorum = 2) locks the slot, matching the main success scenario's majority rule. | Keep votes reaching a majority of connected members lock the slot. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_changing_a_vote_before_quorum_replaces_the_previous_vote_instead_of_adding_to_it` | UC3 — Vote on a Spin Result: This proves Extension 2a: a member switching their vote removes their prior choice rather than being counted under both "keep" and "reroll". | Changing a vote before quorum replaces the previous vote instead of adding to it. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_disconnected_voter_no_longer_counts_toward_the_live_quorum` | UC3 — Vote on a Spin Result: This proves Extension 4a: the same single keep-vote fails to reach quorum among 3 live members (quorum=2) but succeeds once other members disconnect and the live count drops to 1 (quorum=1), since quorum is recomputed from currently connected members. | A disconnected voter no longer counts toward the live quorum. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_party_spin_fills_all_three_slots_and_keeps_locked_dishes_in_place` | UC4 — Spin the Party Slot Machine: This proves that a party spin preserves a locked slot's dish and fills the remaining slots with distinct dishes, matching the main success scenario. | A party spin fills all three slots and keeps locked dishes in place. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_failing_internal_spin_falls_back_to_the_hardcoded_menu` | UC4 — Spin the Party Slot Machine: This proves Extension 4a: when the internal spin call fails on every attempt, all three slots are filled from the hardcoded fallback menu instead of the request failing. | A failing internal spin falls back to the hardcoded menu. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_slot_still_unfilled_after_six_attempts_gets_a_no_options_placeholder` | UC4 — Spin the Party Slot Machine: This proves Extension 5a: when every attempt returns the same already-used dish, the two slots that can never get a unique pick end up with "No options" placeholders after the 6-try limit, rather than looping forever. | A slot still unfilled after six attempts gets a no options placeholder. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_locked_slot_keeps_its_dish_while_other_slots_get_new_ones` | UC5 — Lock a Reel and Re-Spin: This proves that a lock keyed by {index, dishId} is honored on respin while unlocked slots are still populated normally. | A locked slot keeps its dish while other slots get new ones. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_legacy_index_only_lock_is_ignored_and_the_slot_is_re_spun` | UC5 — Lock a Reel and Re-Spin: This proves Extension 3a: a legacy lock payload that carries no usable {index, dishId} pair is treated as no lock at all, so the slot is populated by the normal weighted pick instead of being held. | A legacy index only lock is ignored and the slot is re spun. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_deduplication_can_leave_fewer_than_three_unique_dishes_without_crashing` | UC5 — Lock a Reel and Re-Spin: This proves Extension 6a: when every reel only offers one shared dish, the spin still returns a full three-slot result (showing "whatever is available") instead of raising an error. | Deduplication can leave fewer than three unique dishes without crashing. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_changed_display_name_is_pushed_to_the_database_on_the_next_poll` | UC6 — Update Display Name: This proves the main success scenario: detecting a display-name change on a poll cycle triggers exactly one database update with the new name. | A changed display name is pushed to the database on the next poll. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_an_unchanged_display_name_triggers_no_database_call` | UC6 — Update Display Name: This proves Extension 2a: when Stack Auth reports the same name as last poll, no redundant database write occurs. | An unchanged display name triggers no database call. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_failed_database_update_leaves_the_previously_synced_name_in_place` | UC6 — Update Display Name: This proves Extension 4a: a failed database update does not corrupt the locally tracked synced name, so the next poll cycle can retry cleanly instead of assuming the update succeeded. | A failed database update leaves the previously synced name in place. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_configured_api_key_returns_up_to_two_normalized_video_results` | UC7 — View Recipe Videos for a Dish: This proves the main success scenario caps recipe videos at two results per dish, matching lib/youtube.ts's documented limit. | A configured api key returns up to two normalized video results. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_missing_api_key_returns_a_deterministic_stub_video_instead_of_an_empty_modal` | UC7 — View Recipe Videos for a Dish: This proves Extension 4a: with no YOUTUBE_API_KEY configured, the system returns stub content rather than calling the search function or leaving the modal empty. | A missing api key returns a deterministic stub video instead of an empty modal. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_video_search_failure_for_one_dish_does_not_affect_other_dishes` | UC7 — View Recipe Videos for a Dish: This proves Extension 4b: a per-dish video lookup failure empties only that dish's video list while other dishes' lookups still succeed. | A video search failure for one dish does not affect other dishes. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_an_authenticated_user_saving_a_dish_persists_it_to_their_account` | UC8 — Save a Meal to Favorites: This proves the main success scenario: saving a dish while authenticated updates the local list and persists it to the account in one step. | An authenticated user saving a dish persists it to their account. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_failed_persistence_request_reverts_the_saved_state` | UC8 — Save a Meal to Favorites: This proves Extension 3a: when persistence fails, the returned state matches the prior (unsaved) list rather than the optimistic update, so the UI can revert the heart icon. | A failed persistence request reverts the saved state. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_guests_locally_saved_meals_are_not_carried_into_an_authenticated_profile` | UC8 — Save a Meal to Favorites: This proves Extension 4a: a guest's saved meal exists only in local state, and signing in afterward starts the authenticated profile from an empty saved-meals list rather than importing it. | A guests locally saved meals are not carried into an authenticated profile. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_dishes_containing_an_excluded_allergen_are_removed_from_results` | UC9 — Filter Meals by Allergen: This proves the main success scenario: any dish containing a selected allergen is excluded, while unaffected dishes remain eligible. | Dishes containing an excluded allergen are removed from results. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_excluding_every_dish_in_a_category_still_yields_a_placeholder_via_the_spin_engine` | UC9 — Filter Meals by Allergen: This proves Extension 4a: when an allergen filter eliminates every candidate for a slot, the downstream spin still returns a "No options" placeholder instead of erroring. | Excluding every dish in a category still yields a placeholder via the spin engine. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_signed_in_users_saved_allergens_preselect_the_filter_menu` | UC9 — Filter Meals by Allergen: This proves Extension 5a: opening the filter menu for a signed-in user with saved allergens starts the selection pre-populated from their account rather than empty. | A signed in users saved allergens preselect the filter menu. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_registering_with_a_new_auth_id_creates_a_user_record` | UC10 — Register an Account: This proves the main success scenario: a first-time auth_id causes a new User record to be upserted with empty allergens and saved meals. | Registering with a new auth id creates a user record. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_registering_with_an_auth_id_that_already_exists_returns_the_existing_record_unchanged` | UC10 — Register an Account: This proves Extension 5a: a returning user's auth_id resolves to the existing record instead of creating a duplicate. | Registering with an auth id that already exists returns the existing record unchanged. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_missing_auth_id_creates_no_database_record` | UC10 — Register an Account: This proves Extension 4a: a null/missing auth_id short-circuits to no record being created, matching ensureUserInDB()'s documented null return. | A missing auth id creates no database record. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_the_healthy_powerup_gives_a_healthy_dish_a_higher_score_than_an_unhealthy_one` | UC11 — Apply Power-Ups to Bias Selection: This proves the main success scenario: activating the Healthy power-up weights an eligible healthy dish above a comparable unhealthy one. | The healthy powerup gives a healthy dish a higher score than an unhealthy one. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_combining_healthy_and_cheap_powerups_multiplies_their_individual_effects` | UC11 — Apply Power-Ups to Bias Selection: This proves Extension 2a: multiple active power-ups combine multiplicatively rather than one overriding the other. | Combining healthy and cheap powerups multiplies their individual effects. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_no_dish_matching_an_active_powerup_still_yields_a_selection_instead_of_failing` | UC11 — Apply Power-Ups to Bias Selection: This proves Extension 4a: when no eligible dish satisfies the active power-up, the spin still returns the best available (lower-weighted) dish rather than returning nothing. | No dish matching an active powerup still yields a selection instead of failing. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_saved_ids_are_resolved_against_the_current_catalog_and_can_be_filtered_by_category` | UC12 — Browse Saved Meals: This proves the main success scenario: saved identifiers resolve to full dish records and can be narrowed to a single category. | Saved ids are resolved against the current catalog and can be filtered by category. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_saved_id_no_longer_in_the_catalog_is_silently_omitted` | UC12 — Browse Saved Meals: This proves Extension 2a: a saved dish that has since been removed from the catalog is dropped from the displayed list rather than causing an error. | A saved id no longer in the catalog is silently omitted. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_an_account_with_no_saved_meals_resolves_to_an_empty_list` | UC12 — Browse Saved Meals: This proves Extension 3a: an empty saved-meal collection resolves cleanly to an empty list, which the UI renders as its empty state. | An account with no saved meals resolves to an empty list. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_removing_a_saved_dish_updates_and_persists_the_shortened_list` | UC13 — Remove a Saved Meal: This proves the main success scenario: removing one saved dish leaves the rest intact and persists the updated collection. | Removing a saved dish updates and persists the shortened list. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_failed_removal_request_keeps_the_dish_in_the_prior_collection` | UC13 — Remove a Saved Meal: This proves Extension 3a: a persistence failure returns the prior collection unchanged so a refresh can restore the server's authoritative state instead of losing the item silently. | A failed removal request keeps the dish in the prior collection. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_removing_the_last_saved_meal_leaves_an_empty_collection` | UC13 — Remove a Saved Meal: This proves that removing a user's final saved meal correctly results in the empty-state collection rather than a special-cased error. | Removing the last saved meal leaves an empty collection. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_leaving_a_party_removes_that_members_record_and_keeps_the_rest` | UC14 — Leave a Party: This proves the main success scenario: leaving removes exactly the departing member's record and leaves other members untouched. | Leaving a party removes that members record and keeps the rest. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_the_host_leaving_implicitly_hands_off_to_the_next_connected_member` | UC14 — Leave a Party: This proves Extension 2a: there is no explicit "reassign host" step; once the host's record is gone, host computation simply falls through to the next live member. | The host leaving implicitly hands off to the next connected member. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_member_who_never_left_keeps_their_membership_when_removal_is_only_attempted` | UC14 — Leave a Party: This proves Extension 2b's guarantee in the opposite direction: if a removal cannot be matched to a real member (modeling a failed/rejected server-side removal), the roster is left exactly as it was rather than partially mutated. | A member who never left keeps their membership when removal is only attempted. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_venues_are_returned_per_cuisine_using_the_provided_coordinates` | UC15 — Find Nearby Restaurants After Spinning: This proves the main success scenario: each requested cuisine is searched near the caller's real coordinates with no per-cuisine errors. | Venues are returned per cuisine using the provided coordinates. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_missing_coordinates_fall_back_to_geocoding_the_location_hint` | UC15 — Find Nearby Restaurants After Spinning: This proves Extension 2a/3a: when the browser provides no coordinates (denied or unsupported geolocation), the system geocodes the configured location hint instead of failing. | Missing coordinates fall back to geocoding the location hint. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_one_cuisines_search_failure_does_not_prevent_other_cuisines_from_returning_results` | UC15 — Find Nearby Restaurants After Spinning: This proves Extension 4a: a Places error for one cuisine is isolated to that cuisine's result and error entry, while the other cuisine still succeeds. | One cuisines search failure does not prevent other cuisines from returning results. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_valid_nickname_creates_an_active_party_with_the_creator_as_host` | UC16 — Create Party: This proves the main success scenario: supplying a valid nickname produces an active party with a generated code and the creator registered as host. | A valid nickname creates an active party with the creator as host. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_an_empty_nickname_prevents_party_creation` | UC16 — Create Party: This proves Extension 1a: a blank (or whitespace-only) nickname is rejected before a party is ever created. | An empty nickname prevents party creation. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_hosts_saved_account_allergens_seed_the_new_partys_host_preferences` | UC16 — Create Party: This proves Extension 4a: a signed-in host's previously saved allergens automatically seed their party-member preferences instead of starting empty. | A hosts saved account allergens seed the new partys host preferences. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_joining_with_a_valid_code_adds_a_new_member_to_the_active_party` | UC17 — Join Party: This proves the main success scenario: a valid code and nickname adds the joiner as a new party member and returns their membership details. | Joining with a valid code adds a new member to the active party. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_joining_with_an_unknown_or_inactive_code_is_rejected` | UC17 — Join Party: This proves Extension 2a: an invalid or inactive party code is rejected and no membership record is created. | Joining with an unknown or inactive code is rejected. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_joining_without_stated_preferences_stores_default_empty_preferences` | UC17 — Join Party: This proves Extension 6a: a joiner who supplies no preferences is still given a member record, defaulted to empty preferences rather than being blocked. | Joining without stated preferences stores default empty preferences. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_valid_credentials_establish_a_session_and_load_the_matching_profile` | UC18 — Sign In to an Account: This proves the main success scenario: valid credentials produce a session and load that same identity's saved profile. | Valid credentials establish a session and load the matching profile. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_invalid_credentials_are_rejected_without_creating_a_session` | UC18 — Sign In to an Account: This proves Extension 3a: credentials that do not match any known identity are rejected, and no session or profile access occurs. | Invalid credentials are rejected without creating a session. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_first_time_valid_identity_gets_a_fresh_empty_profile_instead_of_being_blocked` | UC18 — Sign In to an Account: This proves Extension 4a: a valid identity with no prior MealSlot profile has one created on the fly with empty saved data, rather than sign-in failing. | A first time valid identity gets a fresh empty profile instead of being blocked. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_signing_out_clears_the_session_profile_and_client_cache` | UC19 — Sign Out of an Account: This proves the main success scenario: sign-out clears the authenticated session, the in-app profile, and any cached account data in one step. | Signing out clears the session profile and client cache. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_failed_provider_sign_out_does_not_falsely_report_success` | UC19 — Sign Out of an Account: This proves Extension 2a: when the identity provider's sign-out step fails, the local session is left intact and the caller is told it did not succeed, rather than optimistically clearing state. | A failed provider sign out does not falsely report success. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_after_sign_out_an_account_only_page_exposes_no_previous_users_data` | UC19 — Sign Out of an Account: This proves Extension 5a: visiting an account-only view after sign-out returns no data at all rather than leaking the previous user's cached profile. | After sign out an account only page exposes no previous users data. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_non_empty_message_is_broadcast_to_connected_party_members` | UC20 — Chat with Party Members: This proves the main success scenario: sending a message delivers it to every connected member's chat panel. | A non empty message is broadcast to connected party members. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_sending_an_empty_message_does_nothing` | UC20 — Chat with Party Members: This proves Extension 2a: an empty or whitespace-only message is a no-op rather than producing a blank chat entry. | Sending an empty message does nothing. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_without_a_dedicated_realtime_server_a_message_only_reaches_local_browser_tabs` | UC20 — Chat with Party Members: This proves Extension 3a: without NEXT_PUBLIC_WS_URL configured, the chat message reaches only same-machine BroadcastChannel peers and never other devices. | Without a dedicated realtime server a message only reaches local browser tabs. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_saving_new_allergen_selections_persists_them_to_the_account` | UC21 — Manage Dietary Preferences (Sebastian_Use_Cases.md, UC12): This proves the main success scenario: an updated allergen list is written to the user's account record. | Saving new allergen selections persists them to the account. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_a_failed_save_leaves_the_prior_allergen_list_active` | UC21 — Manage Dietary Preferences (Sebastian_Use_Cases.md, UC12): This proves Extension 5a: when the save request fails, the account keeps its previously active allergen list instead of ending up in a partially-updated state. | A failed save leaves the prior allergen list active. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_saved_account_allergens_are_applied_to_a_spin_without_reselecting_them` | UC21 — Manage Dietary Preferences (Sebastian_Use_Cases.md, UC12): This proves the use case's core value: once allergens are saved to the account, they are applied to spin candidates automatically, without the user re-entering them as a session filter (Use Case 9). | Saved account allergens are applied to a spin without reselecting them. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_spin_request_with_a_missing_category_is_rejected_before_selection` | UC1 — Spin for a Meal (additional coverage): New coverage: Sebastian tests an empty candidate reel, but not rejection of a malformed request whose required category value is missing. | Spin request with a missing category is rejected before selection. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_spin_with_multiple_candidates_uses_the_injected_random_position` | UC1 — Spin for a Meal (additional coverage): New coverage: this verifies real weighted selection among several eligible candidates, not Sebastian's one-candidate deterministic scenario. | Spin with multiple candidates uses the injected random position. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_merging_no_party_preferences_produces_no_invented_constraints` | UC2 — Set Party Dietary Preferences and Resolve Conflicts: New coverage: an empty party preference set must not manufacture a diet, allergen, budget, or time restriction. | Merging no party preferences produces no invented constraints. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_duplicate_party_allergens_are_deduplicated_and_sorted` | UC2 — Set Party Dietary Preferences and Resolve Conflicts: New coverage: Sebastian verifies unioning different allergens, while this checks duplicate removal and the deterministic stored ordering. | Duplicate party allergens are deduplicated and sorted. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_reroll_votes_reaching_quorum_release_the_slot_for_another_spin` | UC3 — Vote on a Spin Result: New coverage: Sebastian exercises a keep-vote quorum; this verifies the distinct reroll action. | Reroll votes reaching quorum release the slot for another spin. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_vote_below_quorum_leaves_the_slot_undecided` | UC3 — Vote on a Spin Result: New coverage: a valid vote that does not reach the required majority must not lock or reroll a slot. | Vote below quorum leaves the slot undecided. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_party_fallback_is_used_without_filtering_its_allergens` | UC4 — Spin the Party Slot Machine: New coverage and discrepancy: the current hardcoded fallback is accepted as-is and is not checked against merged party allergens. | Party fallback is used without filtering its allergens. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_party_spin_never_reuses_a_dish_already_held_in_a_locked_slot` | UC4 — Spin the Party Slot Machine: New coverage: the deduplication set includes locked dishes, preventing their reuse in an unlocked slot. | Party spin never reuses a dish already held in a locked slot. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_lock_for_a_dish_absent_from_the_current_reel_is_ignored` | UC5 — Lock a Reel and Re-Spin: New coverage: a structurally valid but stale dish lock falls back to normal selection. | Lock for a dish absent from the current reel is ignored. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_lock_with_an_out_of_range_slot_index_cannot_affect_valid_reels` | UC5 — Lock a Reel and Re-Spin: New coverage: malformed lock positions are discarded rather than being applied to an unrelated reel. | Lock with an out of range slot index cannot affect valid reels. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_missing_provider_display_name_skips_database_synchronization` | UC6 — Update Display Name: New coverage: Sebastian checks an unchanged name, while this checks Stack Auth returning no usable display name. | Missing provider display name skips database synchronization. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_name_sync_can_succeed_on_a_later_poll_after_a_transient_failure` | UC6 — Update Display Name: New coverage: the polling design retries an unsynchronized provider name and can recover on a later cycle. | Name sync can succeed on a later poll after a transient failure. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_successful_video_search_with_no_matches_returns_an_empty_group` | UC7 — View Recipe Videos for a Dish: New coverage: this distinguishes a successful zero-result search from Sebastian's configured-key, fallback, and exception scenarios. | Successful video search with no matches returns an empty group. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_video_result_order_is_preserved_when_limited_to_two_items` | UC7 — View Recipe Videos for a Dish: New coverage: the two-result limit must preserve the provider's ranking rather than selecting arbitrary videos. | Video result order is preserved when limited to two items. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_guest_save_updates_local_collection_without_calling_account_persistence` | UC8 — Save a Meal to Favorites: New coverage: Sebastian checks lack of guest-to-account migration; this verifies the original guest save itself remains local-only. | Guest save updates local collection without calling account persistence. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_selecting_an_already_saved_heart_toggles_the_dish_out_of_favorites` | UC8 — Save a Meal to Favorites: New coverage: the home-page heart is a toggle, so selecting an already saved dish performs the inverse transition. | Selecting an already saved heart toggles the dish out of favorites. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_allergen_exclusion_is_case_insensitive` | UC9 — Filter Meals by Allergen: New coverage: differently capitalized catalog and user values still represent the same safety exclusion. | Allergen exclusion is case insensitive. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_dish_is_excluded_when_any_one_of_its_multiple_allergens_matches` | UC9 — Filter Meals by Allergen: New coverage: a dish is unsafe when any allergen intersects the exclusion set, even when its other allergens are allowed. | Dish is excluded when any one of its multiple allergens matches. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_two_distinct_authentication_identities_create_isolated_profiles` | UC10 — Register an Account: New coverage: registration for separate identities must not overwrite or reuse another diner's profile. | Two distinct authentication identities create isolated profiles. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_registration_without_a_display_name_still_links_the_auth_identity` | UC10 — Register an Account: New coverage: a missing optional display name does not have the same effect as the already-tested missing auth ID. | Registration without a display name still links the auth identity. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_max_thirty_minutes_powerup_scores_a_fast_dish_above_a_slow_dish` | UC11 — Apply Power-Ups to Bias Selection: New coverage: Sebastian checks Healthy and combined Healthy/Cheap; this verifies the distinct preparation-time power-up. | Max thirty minutes powerup scores a fast dish above a slow dish. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_disabling_all_powerups_restores_equal_base_scores` | UC11 — Apply Power-Ups to Bias Selection: New coverage: toggling all power-ups off removes their bias rather than retaining a previous scoring influence. | Disabling all powerups restores equal base scores. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_saved_meals_are_resolved_in_the_users_saved_order` | UC12 — Browse Saved Meals: New coverage: resolving IDs must preserve the user's collection order rather than catalog ordering. | Saved meals are resolved in the users saved order. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_category_filter_with_no_matches_does_not_modify_saved_collection` | UC12 — Browse Saved Meals: New coverage: an empty filtered view is temporary and does not delete the underlying favorites. | Category filter with no matches does not modify saved collection. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_removing_an_unknown_saved_id_persists_the_unchanged_collection` | UC13 — Remove a Saved Meal: New coverage: a stale removal request is idempotent and does not remove an unrelated favorite. | Removing an unknown saved id persists the unchanged collection. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_removing_one_duplicate_reference_eliminates_all_matching_references` | UC13 — Remove a Saved Meal: New coverage: the filtering implementation removes every occurrence of the target identifier from malformed duplicate data. | Removing one duplicate reference eliminates all matching references. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_last_member_leaving_produces_an_empty_party_with_no_computed_host` | UC14 — Leave a Party: New coverage: Sebastian tests host handoff with a remaining member; this verifies the zero-member boundary. | Last member leaving produces an empty party with no computed host. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_current_local_leave_behavior_does_not_remove_authoritative_membership` | UC14 — Leave a Party: New coverage and discrepancy: the inspected page clears and reloads locally without calling the implemented leave endpoint. | Current local leave behavior does not remove authoritative membership. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_failed_geocoding_still_allows_places_results_without_an_origin` | UC15 — Find Nearby Restaurants After Spinning: New coverage: geocoding failure does not automatically discard venue results, but distance remains unavailable. | Failed geocoding still allows places results without an origin. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_empty_cuisine_list_performs_no_places_or_geocoding_work_with_coordinates` | UC15 — Find Nearby Restaurants After Spinning: New coverage: no spun cuisines means there are no external restaurant searches to perform. | Empty cuisine list performs no places or geocoding work with coordinates. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_nickname_longer_than_twenty_four_characters_is_rejected` | UC16 — Create Party: New coverage: Sebastian checks empty input; this checks the opposite nickname-length boundary. | Nickname longer than twenty four characters is rejected. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_whitespace_only_nickname_is_accepted_by_the_current_create_route_model` | UC16 — Create Party: New coverage and discrepancy: server-side length validation does not trim whitespace, despite the browser blocking this value. | Whitespace only nickname is accepted by the current create route model. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_party_code_collision_reports_creation_failure_without_overwriting_existing_party` | UC16 — Create Party: New coverage: a generated-code collision is surfaced rather than silently replacing an existing party. | Party code collision reports creation failure without overwriting existing party. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_joining_with_a_code_of_the_wrong_length_is_rejected_before_lookup` | UC17 — Join Party: New coverage: Sebastian checks an unknown or inactive code, while this checks request-shape validation. | Joining with a code of the wrong length is rejected before lookup. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_repeated_join_requests_create_duplicate_memberships_in_current_behavior` | UC17 — Join Party: New coverage and discrepancy: the current join route has no uniqueness check for an existing party member. | Repeated join requests create duplicate memberships in current behavior. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_authentication_provider_outage_creates_neither_session_nor_profile` | UC18 — Sign In to an Account: New coverage: this exercises provider unavailability, not Sebastian's invalid-credential branch. | Authentication provider outage creates neither session nor profile. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_signing_in_as_another_identity_never_returns_the_first_users_profile` | UC18 — Sign In to an Account: New coverage: profile selection is isolated by authenticated identity and cannot leak another diner's saved meals. | Signing in as another identity never returns the first users profile. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_signing_out_twice_is_idempotent` | UC19 — Sign Out of an Account: New coverage: repeated sign-out actions remain safe and do not restore or corrupt cleared state. | Signing out twice is idempotent. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_sign_out_does_not_delete_unrelated_guest_preferences` | UC19 — Sign Out of an Account: New coverage: account-specific state is cleared without erasing an unrelated browser-level preference. | Sign out does not delete unrelated guest preferences. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_duplicate_realtime_chat_message_is_displayed_only_once` | UC20 — Chat with Party Members: New coverage: retransmission of the same realtime message does not create duplicate visible chat entries. | Duplicate realtime chat message is displayed only once. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_chat_history_is_not_restored_after_a_new_party_client_session` | UC20 — Chat with Party Members: New coverage: the use case states that chat lasts only for the session; a reload has no persisted history to restore. | Chat history is not restored after a new party client session. | PASS — Pytest executed the modeled scenario and every assertion matched. |
| `test_incompatible_party_diets_should_raise_a_conflict` | UC02 — Set Party Dietary Preferences: Verify that incompatible group diets are explicitly reported. | Vegan and omnivore preferences produce a conflict. | **FAIL** — The merge selected vegan and returned `conflict=False`. Current merge logic always selects the strictest ethic diet, making this conflict path unreachable. |
| `test_party_fallback_should_exclude_dishes_with_group_allergens` | UC04 — Spin the Party Slot Machine: Allergen exclusions must remain effective during fallback behavior. | A peanut-excluding party receives no peanut fallback dish. | **FAIL** — The modeled fallback returned `unsafe`, `safe1`, and `safe2`, including the peanut dish. The fallback menu is used without applying merged allergen exclusions. |
| `test_whitespace_only_party_nickname_should_be_rejected` | UC16 — Create Party: Validate a meaningful host nickname before creating party data. | Whitespace-only nickname returns status 400. | **FAIL** — Length-only route validation accepted it with status 200. Server-side validation does not trim the nickname before checking its length. |
| `test_repeated_join_should_not_create_a_duplicate_membership` | UC17 — Join Party: Prevent the same participant from creating repeated memberships. | Second join is rejected with a conflict response. | **FAIL** — Both joins returned 200 and the roster grew to three records. The join path does not enforce unique party membership. |
| `test_leaving_from_the_page_should_remove_the_authoritative_membership` | UC14 — Leave a Party: Ensure Leave Party changes server-authoritative membership. | Departing member `m2` is absent afterward. | **FAIL** — Local state cleared, but authoritative members remained `m1` and `m2`. The inspected page resets locally without invoking the implemented leave endpoint. |
| `test_guest_should_be_able_to_browse_a_locally_saved_meal` | UC08 — Save a Meal to Favorites: Confirm that guest-local saves can be revisited as the use case claims. | Favorites displays locally saved `dish1`. | **FAIL** — The signed-out Favorites behavior exposed an empty collection. Guest saves use local storage, while the Favorites page requires authentication. |
| `test_failed_account_save_should_revert_the_optimistic_saved_state` | UC08 — Save a Meal to Favorites: Keep displayed and persisted saved state consistent after failure. | Failed persistence restores the prior empty collection. | **FAIL** — The optimistic collection continued to contain `dish1`. The current home-page failure path logs the error without rolling back optimistic state. |
| `test_party_chat_should_deliver_the_message_to_another_browser` | UC20 — Chat with Party Members: Verify the central value of realtime party chat. | The remote browser receives `Pizza?`. | **FAIL** — Only the sender log contained the message; the remote log was empty. The inspected raw-WebSocket client protocol does not match the Socket.IO server protocol. |
| `test_default_title_case_category_should_find_seeded_dishes` | UC01 — Spin for a Meal: Verify that the application’s default categories work with its supplied data. | `Breakfast` finds seeded breakfast dishes. | **FAIL** — Exact case-sensitive matching returned no candidates. The UI uses title case while the seed data stores lowercase categories. |
| `test_join_page_should_retain_the_returned_member_identifier` | UC17 — Join Party: Joined members need their identity for preferences, voting, and realtime behavior. | Page state retains returned member ID `m2`. | **FAIL** — Page state stored `memberId=None`. The join page discards the membership identifier returned by the join operation. |
